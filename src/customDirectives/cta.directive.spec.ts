import { CtaDirective } from './cta.directive';

describe('CtaDirective', () => {
  it('should create an instance', () => {
    const directive = new CtaDirective();
    expect(directive).toBeTruthy();
  });
});
